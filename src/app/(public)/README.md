# `(public)` route group

Unauthenticated, indexable surfaces: landing page, `privacy/`, `terms/`.

- Architecture: `docs/02-technical-architecture.md` §6.1
- These are the only routes that may be statically rendered (performance skill).
- All copy here is reviewed against the honesty rules in
  `docs/01-product-requirements.md` — no claim of scanning, guaranteed deletion,
  autonomous sending, or end-to-end encryption.
