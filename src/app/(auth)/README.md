# `(auth)` route group

Sign-in and verification surfaces: `sign-in/`, `verify/`.

- Tickets: ATL-011 (auth setup), ATL-014 (auth UI)
- Frontend spec §16: calm, minimal, neutral error messages, no misleading security claims
- Security §5: responses must never reveal whether an email address is registered
