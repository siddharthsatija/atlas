/**
 * Tailwind CSS v4 uses CSS-first configuration.
 * Design tokens live in src/styles/tokens.css (@theme), not in a tailwind.config.js.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
